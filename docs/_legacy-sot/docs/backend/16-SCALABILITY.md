# Scalability

Vertical first: OPcache, indexes, pagination, image optimization, HTTP cache, query profiling.

Horizontal ready: stateless access validation, shared refresh/session storage, object storage, Redis, queue. Microservices hanya bila bounded module punya independent scaling/organizational need.
