# Uploads

MVP: JPEG, PNG, WebP.

Auth/authz → capability → size → actual MIME/decode → dimension/decompression check → re-encode → random name → non-executable storage → metadata transaction → remove old after success.

SVG, ZIP, PDF, arbitrary files tidak diperbolehkan.
