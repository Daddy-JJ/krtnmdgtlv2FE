# Internationalization

Languages: Indonesian (`id`) dan English (`en`). Semua UI strings memakai locale key. User content tidak diterjemahkan otomatis. Locale disimpan pada card/account. Format tanggal/mata uang menggunakan Intl. Error code dipetakan ke localized message.
