# Frontend Coding Standards

ES modules; const default; small pure functions; event delegation; no inline onclick; no global pollution; semantic HTML; documented component inputs/events; API call isolated in services; visible error states.
