import{resumeService}from'../../services/resume-service.js';const id=new URLSearchParams(location.search).get('id'),form=document.querySelector('[data-form]'),status=document.querySelector('[data-status]');form.addEventListener('submit',async e=>{e.preventDefault();status.textContent='Mengirim...';try{await resumeService.revision(id,new FormData(form).get('notes'));location.assign(`/app/resume-enhancement/request/?id=${encodeURIComponent(id)}`)}catch(error){status.textContent=error.message}});
